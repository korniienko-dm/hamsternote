from django.apps import AppConfig


class SharingNotesConfig(AppConfig):
    """
    AppConfig class for the 'sharing_notes' app.
    This class defines configuration for the 'sharing_notes' app.
    """
    default_auto_field = 'django.db.models.BigAutoField'
    name = 'sharing_notes'
