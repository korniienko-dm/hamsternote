from django.apps import AppConfig


class UserContactsConfig(AppConfig):
    """
    AppConfig for managing user contacts.
    """
    default_auto_field = 'django.db.models.BigAutoField'
    name = 'user_contacts'
