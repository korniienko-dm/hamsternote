from django.apps import AppConfig


class RegistrationConfig(AppConfig):
    """
    Configuration class for the 'registration' app.
    """
    default_auto_field = 'django.db.models.BigAutoField'
    name = 'registration'
